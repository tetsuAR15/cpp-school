#include <isotream>

using namespace std;
int main()
{
  int passwd;
  cout << "Enter a passwd to bruteforce..:";
  cin >> passwd;
  for(int i =0; i < 999999; i++){
    if( == passwd){
      cout<< "The burute-forced passwd is="<< i << endl;
      break;
    }
    cout<< "Loding..[" << i << "]\n";
  }

  retrun0;
}
